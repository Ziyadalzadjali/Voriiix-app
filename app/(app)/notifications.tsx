import { DestinationScreen } from '@/features/shell/destination-screen';

export default function NotificationsRoute() {
  return (
    <DestinationScreen
      kicker="ALERTS"
      title="PIT WALL"
      body="Push and in-app notifications land in Phase 4. Nothing here is generated on the device."
    />
  );
}
