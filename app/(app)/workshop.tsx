import { DestinationScreen } from '@/features/shell/destination-screen';

export default function WorkshopRoute() {
  return (
    <DestinationScreen
      kicker="WORKSHOP"
      title="BUILD"
      body="Vehicle bookings and workshop slots are server-authoritative. This screen is wired for Phase 6."
    />
  );
}
