import { DestinationScreen } from '@/features/shell/destination-screen';

export default function AcademyRoute() {
  return (
    <DestinationScreen
      kicker="ACADEMY"
      title="SKILL PATH"
      body="Levels, coaches and progress load from the backend in Phase 6. The client never awards XP."
    />
  );
}
