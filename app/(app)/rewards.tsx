import { DestinationScreen } from '@/features/shell/destination-screen';

export default function RewardsRoute() {
  return (
    <DestinationScreen
      kicker="REWARDS"
      title="UNLOCK"
      body="Reward grants are created on the server. The app only displays what the API returns."
    />
  );
}
